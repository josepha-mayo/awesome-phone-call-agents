# One supported, consenting live test: still pending

Status on 9 September 2026: The tested CALL-E MCP interface returned NG/English unsupported and `ready_to_run=false`. No run was started. The SDK/API README lists NG; that is a documented alternative, not a verified successful account or call. The support thread has no incoming reply at the last read.

A real completion needs a trusted local agent or user to authenticate through the official CALL-E dashboard/CLI. Do not paste credentials into ChatGPT, a pull request, browser form, screenshot or this document. Configure an API key only in the local server environment, outside source control. Confirm the correct CALL-E account email in that authenticated account, not by assuming it matches Gmail.

Use an explicitly authorized recipient in a supported region, with truthful country and timezone details. Never borrow an unrelated number, use false country data, or turn a role-play into an actual merchant claim. The caller must identify itself as an AI and ask permission for a short recorded test; stop on decline. The separate `--live` flag and exact recipient confirmation are required. The supplied timezone must be true and the call must be during 09:00–18:00 there.

Test script: a disclosed fictional return enquiry. The recipient supplies an authorization reference, destination, payer, absolute deadline and refund/replacement condition. Ask for a read-back. A later deliberate address correction tests whether the returned transcript is retained. No payment, refund initiation, booking, account change or shipping commitment is allowed. Use no personal order information. One call only; no automatic follow-up/redial.

Record the actual provider call ID and terminal result locally. Verify schema adaptation against the actual transcript, preserving speaker attribution and later corrections. Redact phone numbers, caller/account IDs and other private identifiers from any public demonstration. Recording/publication of a recipient voice requires their separate agreement. A sanitized provider trace is evidence of that run, not a guarantee about future calls.

If the API refuses this region/account too, preserve the exact non-secret error, leave the entry blocked and do not change geography. If the create request is ambiguous, reconcile the existing intent in the provider dashboard rather than redial. If there is a known call ID, resume polling it; no new POST is required.

Only after the real test and result review should the final competition demonstration describe successful CALL-E integration. The current synthetic UI recording is separate.
